<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Cellular.SmartDot">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Image
                Source="logo.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="RevMetrix Bowling Ball Logo" />

            <Label
                Text="SmartDot"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                x:Name="StatusLabel"
                Text="Ready to scan"
                FontSize="16"
                TextColor="Gray" />

            <!-- Scan Button -->
            <Button
                x:Name="ScanButton"
                Text="Scan"
                Clicked="OnScanClicked"
                HorizontalOptions="Fill" />

            <!-- Device List -->
            <Label
                Text="Available Devices:"
                FontSize="18"
                FontAttributes="Bold" />

            <CollectionView
                x:Name="DeviceListView"
                ItemsSource="{Binding Devices}"
                SelectionMode="Single">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            
                            <Label
                                Grid.Row="0"
                                Grid.Column="0"
                                Text="{Binding DisplayName}"
                                FontSize="16"
                                FontAttributes="Bold" />
                            
                            <Label
                                Grid.Row="1"
                                Grid.Column="0"
                                Text="{Binding Id}"
                                FontSize="12"
                                TextColor="Gray" />
                            
                            <Label
                                Grid.Row="0"
                                Grid.Column="1"
                                Text="{Binding Rssi, StringFormat='{0} dBm'}"
                                FontSize="14"
                                TextColor="Gray"
                                VerticalOptions="Center" />
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <!-- Connect/Disconnect Buttons -->
            <Button
                x:Name="ConnectButton"
                Text="Connect"
                Clicked="OnConnectClicked"
                IsEnabled="{Binding CanConnect}"
                HorizontalOptions="Fill" />

            <Button
                x:Name="DisconnectButton"
                Text="Disconnect"
                Clicked="OnDisconnectClicked"
                IsEnabled="{Binding CanDisconnect}"
                HorizontalOptions="Fill" />

            <!-- Device Info Section -->
            <Frame x:Name="DeviceInfoFrame"
                   IsVisible="False"
                   BorderColor="LightGray"
                   Padding="12"
                   CornerRadius="8"
                   HasShadow="False">
                <VerticalStackLayout Spacing="6">
                    <Label Text="Device Info"
                           FontSize="16"
                           FontAttributes="Bold" />

                    <Grid ColumnDefinitions="Auto,*" RowDefinitions="Auto,Auto,Auto,Auto,Auto,Auto" ColumnSpacing="12" RowSpacing="6">
                        <Label Grid.Row="0" Grid.Column="0" Text="Model:" FontSize="14" FontAttributes="Bold" TextColor="Gray"/>
                        <Label Grid.Row="0" Grid.Column="1" x:Name="DeviceModelLabel" Text="-" FontSize="14"/>

                        <Label Grid.Row="1" Grid.Column="0" Text="Serial:" FontSize="14" FontAttributes="Bold" TextColor="Gray"/>
                        <Label Grid.Row="1" Grid.Column="1" x:Name="DeviceSerialLabel" Text="-" FontSize="14"/>

                        <Label Grid.Row="2" Grid.Column="0" Text="Hardware:" FontSize="14" FontAttributes="Bold" TextColor="Gray"/>
                        <Label Grid.Row="2" Grid.Column="1" x:Name="DeviceHardwareLabel" Text="-" FontSize="14"/>

                        <Label Grid.Row="3" Grid.Column="0" Text="Battery:" FontSize="14" FontAttributes="Bold" TextColor="Gray"/>
                        <Label Grid.Row="3" Grid.Column="1" x:Name="DeviceBatteryLabel" Text="-" FontSize="14"/>

                        <Label Grid.Row="4" Grid.Column="0" Text="Firmware:" FontSize="14" FontAttributes="Bold" TextColor="Gray"/>
                        <Label Grid.Row="4" Grid.Column="1" x:Name="DeviceFirmwareLabel" Text="-" FontSize="14"/>

                        <Label Grid.Row="5" Grid.Column="0" Text="Status:" FontSize="14" FontAttributes="Bold" TextColor="Gray"/>
                        <Label Grid.Row="5" Grid.Column="1" x:Name="DeviceFirmwareStatusLabel" Text="-" FontSize="14"/>
                    </Grid>
                </VerticalStackLayout>
            </Frame>

            <!-- Light Sensor Threshold Setting -->
            <Label
                Text="Light Sensor Record Threshold:"
                FontSize="16"
                FontAttributes="Bold"
                Margin="0,20,0,0" />
            <Label
                Text="Recording triggers when the light sensor reading exceeds this value."
                FontSize="13"
                TextColor="Gray" />
            <Grid ColumnDefinitions="*,Auto" ColumnSpacing="10">
                <Entry x:Name="LightThresholdEntry"
                       Grid.Column="0"
                       Keyboard="Numeric"
                       Placeholder="e.g. 25000"
                       ReturnType="Done" />
                <Button x:Name="SaveThresholdButton"
                        Grid.Column="1"
                        Text="Save"
                        Clicked="OnSaveLightThresholdClicked"
                        WidthRequest="80" />
            </Grid>
            <Label x:Name="LightThresholdStatusLabel"
                   FontSize="13"
                   TextColor="Gray" />

            <!-- Record Duration Setting -->
            <Label
                Text="Record Duration (seconds):"
                FontSize="16"
                FontAttributes="Bold"
                Margin="0,15,0,0" />
            <Label
                Text="How long data is recorded after the light sensor threshold is hit."
                FontSize="13"
                TextColor="Gray" />
            <Grid ColumnDefinitions="*,Auto" ColumnSpacing="10">
                <Entry x:Name="RecordDurationEntry"
                       Grid.Column="0"
                       Keyboard="Numeric"
                       Placeholder="e.g. 4"
                       ReturnType="Done" />
                <Button x:Name="SaveDurationButton"
                        Grid.Column="1"
                        Text="Save"
                        Clicked="OnSaveRecordDurationClicked"
                        WidthRequest="80" />
            </Grid>
            <Label x:Name="RecordDurationStatusLabel"
                   FontSize="13"
                   TextColor="Gray" />

            <!-- Device Controls -->
            <VerticalStackLayout x:Name="DeviceControlsSection" IsVisible="False" Spacing="0">
            <Label Text="Device Controls"
                   FontSize="18"
                   FontAttributes="Bold"
                   Margin="0,20,0,5" />
            <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                <Button x:Name="ResetDeviceButton"
                        Grid.Column="0"
                        Text="Reset Device"
                        Clicked="OnResetDeviceClicked"
                        IsEnabled="{Binding IsConnected}"
                        BackgroundColor="#E74C3C" />
                <Button x:Name="SleepModeButton"
                        Grid.Column="1"
                        Text="Sleep Mode"
                        Clicked="OnSleepModeClicked"
                        IsEnabled="{Binding IsConnected}"
                        BackgroundColor="#3498DB" />
            </Grid>
            </VerticalStackLayout>

            <!-- Sensor Configuration -->
            <VerticalStackLayout x:Name="SensorConfigSection" IsVisible="False" Spacing="0">
            <Label Text="Sensor Configuration"
                   FontSize="18"
                   FontAttributes="Bold"
                   Margin="0,20,0,5" />

            <!-- Accelerometer Config -->
            <Frame BorderColor="LightGray" Padding="12" CornerRadius="8" HasShadow="False">
                <VerticalStackLayout Spacing="6">
                    <Label Text="Accelerometer" FontSize="16" FontAttributes="Bold" />
                    <Label Text="Scale (G):" FontSize="14" TextColor="Gray" />
                    <Picker x:Name="AccelScalePicker" Title="Select Scale" />
                    <Label Text="Frequency (Hz):" FontSize="14" TextColor="Gray" />
                    <Picker x:Name="AccelFreqPicker" Title="Select Frequency" />
                </VerticalStackLayout>
            </Frame>

            <!-- Ambient Light Config -->
            <Frame BorderColor="LightGray" Padding="12" CornerRadius="8" HasShadow="False" Margin="0,8,0,0">
                <VerticalStackLayout Spacing="6">
                    <Label Text="Ambient Light" FontSize="16" FontAttributes="Bold" />
                    <Label Text="Gain:" FontSize="14" TextColor="Gray" />
                    <Picker x:Name="LightGainPicker" Title="Select Gain" />
                    <Label Text="Integration Time:" FontSize="14" TextColor="Gray" />
                    <Picker x:Name="LightIntegTimePicker" Title="Select Integration Time" />
                    <Label Text="Sampling Rate:" FontSize="14" TextColor="Gray" />
                    <Picker x:Name="LightSamplingRatePicker" Title="Select Sampling Rate" />
                </VerticalStackLayout>
            </Frame>

            <!-- Barometer Config -->
            <Frame BorderColor="LightGray" Padding="12" CornerRadius="8" HasShadow="False" Margin="0,8,0,0">
                <VerticalStackLayout Spacing="6">
                    <Label Text="Barometer" FontSize="16" FontAttributes="Bold" />
                    <Label Text="Oversampling:" FontSize="14" TextColor="Gray" />
                    <Picker x:Name="BaroOversamplingPicker" Title="Select Oversampling" />
                    <Label Text="Averaging (IIR Filter):" FontSize="14" TextColor="Gray" />
                    <Picker x:Name="BaroAveragingPicker" Title="Select Averaging" />
                    <Label Text="Standby Time:" FontSize="14" TextColor="Gray" />
                    <Picker x:Name="BaroStandbyTimePicker" Title="Select Standby Time" />
                </VerticalStackLayout>
            </Frame>

            <!-- Gyroscope Config -->
            <Frame BorderColor="LightGray" Padding="12" CornerRadius="8" HasShadow="False" Margin="0,8,0,0">
                <VerticalStackLayout Spacing="6">
                    <Label Text="Gyroscope" FontSize="16" FontAttributes="Bold" />
                    <Label Text="Frequency (Hz):" FontSize="14" TextColor="Gray" />
                    <Picker x:Name="GyroFreqPicker" Title="Select Frequency" />
                    <Label Text="Range (°/s):" FontSize="14" TextColor="Gray" />
                    <Picker x:Name="GyroRangePicker" Title="Select Range" />
                </VerticalStackLayout>
            </Frame>

            <!-- Magnetometer Config -->
            <Frame BorderColor="LightGray" Padding="12" CornerRadius="8" HasShadow="False" Margin="0,8,0,0">
                <VerticalStackLayout Spacing="6">
                    <Label Text="Magnetometer" FontSize="16" FontAttributes="Bold" />
                    <Label x:Name="MagFrequencyLabel" FontSize="14" TextColor="Gray" />
                </VerticalStackLayout>
            </Frame>

            <Button x:Name="SaveSensorConfigButton"
                    Text="Save Sensor Configuration"
                    Clicked="OnSaveSensorConfigClicked"
                    Margin="0,8,0,0" />
            </VerticalStackLayout>

            <!-- Sensor Controls -->
            <Label
                Text="Sensors:"
                FontSize="18"
                FontAttributes="Bold"
                Margin="0,20,0,0" />

            <Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto,Auto,Auto" ColumnSpacing="10" RowSpacing="10">
                <Label
                    Grid.Row="0"
                    Grid.Column="0"
                    Grid.ColumnSpan="2"
                    Text="Accelerometer"
                    FontSize="16"
                    FontAttributes="Bold" />

                <Button
                    Grid.Row="1"
                    Grid.Column="0"
                    Text="Start"
                    Clicked="OnStartAccelerometerClicked"
                    IsEnabled="{Binding CanStartAccelerometer}" />

                <Button
                    Grid.Row="1"
                    Grid.Column="1"
                    Text="Stop"
                    Clicked="OnStopAccelerometerClicked"
                    IsEnabled="{Binding CanStartAccelerometer}" />

                <Label
                    Grid.Row="2"
                    Grid.Column="0"
                    Grid.ColumnSpan="2"
                    x:Name="AccelerometerLabel"
                    Text=""
                    FontSize="14" />

                <Label
                    Grid.Row="3"
                    Grid.Column="0"
                    Grid.ColumnSpan="2"
                    Text="Gyroscope"
                    FontSize="16"
                    FontAttributes="Bold"
                    Margin="0,10,0,0" />
            </Grid>

            <Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto" ColumnSpacing="10" RowSpacing="10">
                <Button
                    Grid.Row="0"
                    Grid.Column="0"
                    Text="Start"
                    Clicked="OnStartGyroscopeClicked"
                    IsEnabled="{Binding CanStartGyroscope}" />

                <Button
                    Grid.Row="0"
                    Grid.Column="1"
                    Text="Stop"
                    Clicked="OnStopGyroscopeClicked"
                    IsEnabled="{Binding CanStartGyroscope}" />

                <Label
                    Grid.Row="1"
                    Grid.Column="0"
                    Grid.ColumnSpan="2"
                    x:Name="GyroscopeLabel"
                    Text=""
                    FontSize="14" />
            </Grid>

            <Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto,Auto" ColumnSpacing="10" RowSpacing="10">
                <Label
                    Grid.Row="0"
                    Grid.Column="0"
                    Grid.ColumnSpan="2"
                    Text="Magnetometer"
                    FontSize="16"
                    FontAttributes="Bold"
                    Margin="0,10,0,0" />

                <Button
                    Grid.Row="1"
                    Grid.Column="0"
                    Text="Start"
                    Clicked="OnStartMagnetometerClicked"
                    IsEnabled="{Binding CanStartMagnetometer}" />

                <Button
                    Grid.Row="1"
                    Grid.Column="1"
                    Text="Stop"
                    Clicked="OnStopMagnetometerClicked"
                    IsEnabled="{Binding CanStartMagnetometer}" />

                <Label
                    Grid.Row="2"
                    Grid.Column="0"
                    Grid.ColumnSpan="2"
                    x:Name="MagnetometerLabel"
                    Text=""
                    FontSize="14" />
            </Grid>

            <Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto,Auto" ColumnSpacing="10" RowSpacing="10">
                <Label
                    Grid.Row="0"
                    Grid.Column="0"
                    Grid.ColumnSpan="2"
                    Text="Light Sensor"
                    FontSize="16"
                    FontAttributes="Bold"
                    Margin="0,10,0,0" />

                <Button
                    Grid.Row="1"
                    Grid.Column="0"
                    Text="Start"
                    Clicked="OnStartLightSensorClicked"
                    IsEnabled="{Binding CanStartLightSensor}" />

                <Button
                    Grid.Row="1"
                    Grid.Column="1"
                    Text="Stop"
                    Clicked="OnStopLightSensorClicked"
                    IsEnabled="{Binding CanStartLightSensor}" />

                <Label
                    Grid.Row="2"
                    Grid.Column="0"
                    Grid.ColumnSpan="2"
                    x:Name="LightSensorLabel"
                    Text=""
                    FontSize="14" />
            </Grid>

            <!-- Probe Button -->
            <Button
                x:Name="ProbeButton"
                Text="Probe Device (Debug)"
                Clicked="OnProbeClicked"
                IsEnabled="{Binding IsConnected}"
                HorizontalOptions="Fill"
                Margin="0,20,0,0"
                BackgroundColor="Orange" />

            <!-- Graph Button -->
            <Button
                x:Name="ShowGraphButton"
                Text="Show Sensor Graph"
                Clicked="OnShowGraphClicked"
                IsEnabled="{Binding IsConnected}"
                HorizontalOptions="Fill"
                Margin="0,10,0,0" />

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>

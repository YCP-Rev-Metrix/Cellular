<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Cellular.Stats"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:cv="clr-namespace:Camera.MAUI;assembly=Camera.MAUI"
             xmlns:vm="clr-namespace:Cellular.ViewModel">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">

            <Image Source="logo.png" HeightRequest="185" Aspect="AspectFit" />

            <Grid HorizontalOptions="Center">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackLayout Grid.Column="0" Grid.Row="0">
                    <DatePicker MinimumDate="{Binding MinDate}"
                                MaximumDate="{Binding MaxDate}"
                                Date="{Binding SelectedDate}"
                                x:Name="StartDate"
                                WidthRequest="100"/>

                    <!-- Sessions: shows sessions for current user -->
                    <Picker x:Name="SessionPicker"
                            Title="Session"
                            ItemsSource="{Binding Sessions}"
                            ItemDisplayBinding="{Binding SessionNumber}"
                            SelectedItem="{Binding SelectedSession}" />

                    <!-- Games: shows games for user or filtered by selected session -->
                    <Picker x:Name="GamePicker"
                            Title="Game"
                            ItemsSource="{Binding Games}"
                            ItemDisplayBinding="{Binding GameNumber}"
                            SelectedItem="{Binding SelectedGame}" />

                    <!-- Lanes: 1-32 -->
                    <Picker x:Name="LanesPicker"
                            Title="Lanes"
                            ItemsSource="{Binding Lanes}"
                            SelectedItem="{Binding SelectedLane}" />
                </StackLayout>

                <StackLayout Grid.Column="2" Grid.Row="0">
                    <DatePicker MinimumDate="{Binding MinDate}"
                                MaximumDate="{Binding MaxDate}"
                                Date="{Binding SelectedDate}"
                                x:Name="EndDate"
                                WidthRequest="100"/>

                    <Picker x:Name="EventPicker"
                            Title="Event"
                            ItemsSource="{Binding Events}"
                            ItemDisplayBinding="{Binding Name}"
                            SelectedItem="{Binding SelectedEvent}" />

                    <!-- Balls: only balls for current user -->
                    <Picker x:Name="BallPicker"
                            Title="Ball"
                            ItemsSource="{Binding Balls}"
                            ItemDisplayBinding="{Binding Name}"
                            SelectedItem="{Binding SelectedBall}" />

                    <!-- Frames: 1-10 -->
                    <Picker x:Name="FramePicker"
                            Title="Frame"
                            ItemsSource="{Binding Frames}"
                            SelectedItem="{Binding SelectedFrame}" />
                </StackLayout>

                <StackLayout Grid.Column="1" Grid.Row="1" VerticalOptions="Center">
                    <Picker x:Name="StatTypePicker"
                            Title="Stat Type"
                            ItemsSource="{Binding StatTypes}"
                            SelectedItem="{Binding SelectedStatType}" />
                    <Button x:Name="LoadStatsButton" Text="Load Stats" WidthRequest="100" />
                </StackLayout>
            </Grid>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
